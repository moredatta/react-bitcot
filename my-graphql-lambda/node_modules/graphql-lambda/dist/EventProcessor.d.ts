import { Context, SQSHandler } from 'aws-lambda';
import { IEventProcessor } from './types';
import { Server } from './Server';
interface EventProcessorOptions {
    onError?: (err: any) => void;
    /**
     * Enable console.log
     */
    debug?: boolean;
}
/**
 * DynamoDBEventProcessor
 *
 * Processes DynamoDB stream event in order to send events to subscribed clients
 */
export declare class EventProcessor<TServer extends Server = Server> implements IEventProcessor<TServer, SQSHandler> {
    private onError;
    private debug;
    constructor(options?: EventProcessorOptions);
    createHandler(server: TServer): (lambdaEvent: any, lambdaContext: Context) => Promise<void>;
}
export {};
